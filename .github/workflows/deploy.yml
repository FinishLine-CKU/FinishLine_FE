name: Frontend Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20.15.0

      - name: Install dependencies
        run: yarn install

      - name: Build React app
        run: CI=false yarn build

      - name: Decode PEM KEY & Access EC2
        run: |
          echo "$EC2_KEY" | base64 -d > FinishLine-Server.pem
          chmod 600 FinishLine-Server.pem
        env:
          EC2_KEY: ${{ secrets.EC2_KEY }}

      - name: Add EC2 host to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H $EC2_HOST >> ~/.ssh/known_hosts
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}

      - name: Setting Permission
        run: ssh -i FinishLine-Server.pem $EC2_USER@$EC2_HOST 'sudo chown -R ubuntu:ubuntu $DEPLOY_PATH'
        env:
          EC2_USER: ${{ secrets.EC2_USER }}
          EC2_HOST: ${{ secrets.EC2_HOST }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        
      - name: Update build files to EC2
        run: |
          scp -i FinishLine-Server.pem -r build/* $EC2_USER@$EC2_HOST:$DEPLOY_PATH
        env:
          EC2_USER: ${{ secrets.EC2_USER }}
          EC2_HOST: ${{ secrets.EC2_HOST }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}

      - name: Restart Nginx on EC2
        run: |
          ssh -i FinishLine-Server.pem $EC2_USER@$EC2_HOST 'sudo systemctl restart nginx'
        env:
          EC2_USER: ${{ secrets.EC2_USER }}
          EC2_HOST: ${{ secrets.EC2_HOST }}
    
      
