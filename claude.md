# CLAUDE.md
# 이 파일은 이 저장소의 코드를 작업할 때 Claude Code (claude.ai/code)가 따라야 할 가이드라인을 제공합니다.

> 작성일 : 2026-08-15
> 대상 : Finishline_fe claude 프론트엔드 개발 문서
> 기술 스택 : React, JavaScript, Aphrodite

## 프로젝트 개요
이 프로젝트는 한국 가톨릭관동대학교 학생들이 만든 졸업요건 진단 사이트의 프론트엔드 폴더입니다.
애플리케이션은 한국어 기반으로 교과목 등록, 졸업요건 계산 기능이 있으며
웹사이트 이름은 "FinishLine-CKU"입니다.

프로젝트는 Create React App(CRA)을 기반으로 하며,
스타일링은 CSS-in-JS 라이브러리인 Aphrodite를 사용합니다.

## 개발 명령어

### 애플리케이션 실행
```bash
yarn start
```
# http://localhost:3000 에서 개발 서버 실행

### 테스트
```bash
yarn test
```
# 인터랙티브 watch 모드로 테스트 실행
```bash
yarn test --coverage
```
# 테스트 커버리지 리포트 생성

### 빌드
```bash
yarn build
```
# build/ 폴더에 프로덕션용 빌드 생성

## 아키텍처

### 스타일링 방식
이 프로젝트는 일반 CSS 파일을 사용하지 않고
Aphrodite(CSS-in-JS)만 사용합니다.

- 모든 스타일은 StyleSheet.create()로 정의
- css() 함수로 컴포넌트에 적용

중요:
- Aphrodite 스타일을 일반 CSS로 변환하지 마세요

## 반응형 디자인
- 모바일: < 768px
- 태블릿: 768px ~ 1200px
- 데스크톱: > 1200px
- 최대 콘텐츠 너비: 1400px

## 네이밍 규칙
- 컴포넌트: PascalCase
- 컴포넌트 파일: .jsx
- 유틸/데이터: .js
- UI 텍스트 및 상품명은 한국어 사용
- 변수/함수/상태: CamelCase

## 주요 규칙 (AI 행동 지침)

- 이 프로젝트의 **최우선 목표는 모바일 환경(반응형) UI 최적화**입니다.  
  기존 UI 요소를 존중하며, **재배치 및 레이아웃 조정**을 통해  
  모바일 환경에서도 사용성이 좋은 UI를 구현해야 합니다.

- 모든 화면은 **모바일 / 태블릿 / 데스크톱** 환경을 모두 고려한  
  반응형 디자인으로 구현되어야 합니다.

- 구현은 **이미 완성된 UI(CSS 기준)** 를 기준으로 하며,  
  명시적으로 요청되지 않은 UI 요소, 버튼, 텍스트, 기능은  
  임의로 추가하지 않습니다.

- 단, **모바일 환경에서 기존 레이아웃이 구조적으로 성립하지 않는 경우에 한해**,  
  **동일한 정보와 기능을 유지한 상태에서 레이아웃만 재구성한 새 디자인을 허용합니다.**

---

## 절대 하지 말 것 (Forbidden Actions)

- 새로운 버튼, 텍스트, 기능 임의 추가
- 기존 디자인 의도와 다른 임의 변경
- 반응형과 무관한 구조·스타일 수정

---

## 반드시 지킬 것 (Required Actions)

- 기존 UI 구조와 디자인 의도를 최우선으로 존중합니다.
- 모든 변경은 **반응형 대응 목적**으로만 수행합니다.
- 판단이 어려울 경우, 임의로 모바일의 경우만 새 디자인을 적용합니다.